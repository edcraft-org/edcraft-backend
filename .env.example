# Application Configuration
ENVIRONMENT=development

# Database Configuration
# Format: postgresql+asyncpg://username:password@host:port/database_name
DATABASE_URL=postgresql+asyncpg://edcraft_user:edcraft_password@localhost:5432/edcraft

# PostgreSQL Settings (used by docker-compose.yml)
# These values are used when running the local PostgreSQL container
POSTGRES_DB=edcraft
POSTGRES_USER=edcraft_user
POSTGRES_PASSWORD=edcraft_password
POSTGRES_PORT=5432
